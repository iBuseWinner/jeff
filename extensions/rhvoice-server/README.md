# RHVoice Text-to-Speech service

This extension uses speechd service written for Linux. Before starting the extension, execute this:

```bash
sudo apt install python3-speechd
```

or this:

```bash
sudo pacman -S libspeechd
```
